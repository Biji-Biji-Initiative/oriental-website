## Merge-gate assessment

I found **no unresolved merge blocker in the supplied contract, specification, and implementation evidence** for reviewed tip `8cf7add52d6f13737e112cc9fcce62e63d96580e`.

| Blocker class                  | Assessment                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Data loss or hard deletion** | **Pass.** The schema change is additive and optional. Archive records the prior status and provenance without removing customer details, transcripts, notification results, ClickUp references, evaluations, or audit history. The archive mutation contains no `ctx.db.delete`, and the exhaustive public API contract introduces no lead-delete route. Restore preserves the canonical record and appends another event. |
| **Archive-boundary bypass**    | **Pass.** Ordinary workflow PATCH no longer accepts `archived`, and the Convex mutation independently rejects both a requested archived status and any canonically archived lead before its first patch. Archived rows expose Restore rather than Edit. This prevents active-to-archived and archived-to-active transitions through the general editor.                                                                    |
| **Partial batch mutation**     | **Pass.** Archive/restore validates batch size, duplicate IDs, reason, existence, state, and every expected revision before applying any patch. The operation is one Convex mutation, with missing, stale, or invalid targets documented to leave the whole batch unchanged. Atomic bulk assignment retains the equivalent validate-before-write contract.                                                                 |
| **Stale-write overwrite**      | **Pass.** Individual workflow edits, bulk assignment, archive, and restore carry expected revisions. Conflicts return `409` without overwriting fields. Archive and restore also increment `workflowRevision`, so a stale editor cannot silently overwrite either transition.                                                                                                                                              |
| **Permission bypass**          | **Pass.** The route requires authenticated admin bearer/cookie access and the explicit `leads.archive` permission. Admin and operator roles receive that permission; viewers remain read-only. Actor identity and request ID come from the authenticated server path rather than from operator-supplied archive data. The underlying Convex functions also require the server-side ingest secret.                          |
| **Incorrect canonical counts** | **Pass.** `adminLeadCounts` is independent of the bounded `adminLeadTable` query and counts the complete, unfiltered canonical collection. Filtering changes only the visible count. Evidence includes a regression with two returned rows versus 720 canonical rows and a browser regression where one filtered row remains three canonical rows. The secret check occurs before collection access.                       |
| **Unusable table or actions**  | **Pass.** The implementation covers search, dedicated filters, stable sorting, column visibility, pagination, selection, row actions, workflow editing, bulk assignment, and archive/restore. Desktop receives a semantic sticky table and mobile receives complete cards with equivalent controls. Required dialog fields prevent malformed submissions.                                                                  |
| **Mouse or touch failure**     | **Pass.** The two Base UI failures discovered during browser review were corrected: menu labels now use the required group context, and controlled pointer-up handling covers touch-only triggers. Dedicated desktop and Pixel 7 regressions cover both Columns and row-action menus. Webwright reports zero horizontal overflow at desktop and 412-pixel mobile widths.                                                   |
| **Public leakage**             | **Pass.** Lead rows, counts, archive actors, reasons, and other operator metadata remain behind the protected admin route. The Convex count query requires the server-only secret before reading and is called through the server adapter. No new data is added to public health or client-configuration responses.                                                                                                        |
| **Unsafe release sequencing**  | **Pass for a pre-merge gate.** The branch correctly claims no Convex, staging, or production deployment. The stated post-merge order—deploy additive Convex schema/functions first, then run managed preflight and exact-merged-SHA staging verification before production—is the safe dependency order. Final merged-SHA deployment proof is properly deferred rather than falsely claimed.                               |

The intentionally skipped pre-existing mobile mutation scenario does not overturn the result: mutation integrity is covered by route and Convex tests, while the feature-specific mobile rendering, dialogs, row actions, touch menus, and overflow behavior have separate Pixel-class browser evidence.

The reported verification set is internally consistent: lint, strict type checking, 377 automated tests, 48 focused integrity tests, production build, desktop/mobile Playwright coverage, Webwright inspection, and `git diff --check` all passed. No evidence contradicts the API contract or the required archive, count, permission, and revision semantics.

VERDICT: MERGE CRM TABLE
