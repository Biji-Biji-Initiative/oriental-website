The provided implementation evidence and source code successfully satisfy all constraints of the voice intake release contract. Here is the trace of the required release components:

*   **Failure Classification & Retry Selection:** `realtime_busy` classification isolates capacity from quota/transport failures, and the jittered retry hook correctly executes only for `realtime_busy` while rejecting exhausted quotas (.apr/evidence/oriental-voice-intake-release.md:5-8, 158-161).
*   **Typed Interruption:** Typed turns serialize exactly as cancel, clear, text, response; expected no-active-response cancellations bypass actionable alerts (.apr/evidence/oriental-voice-intake-release.md:18-19).
*   **Adaptive/Strict Grounding & Bounded ASR:** Adaptive capture applies syntax validation, evidence grounding, and bounded edit distances (`<= min(3, floor(18% length))` for emails). The native-audio identity grounds to a 1-edit phonetic skeleton with a negative test proof. Strict mode enforces explicit confirmation, and prior evidence invalidates on corrections (.apr/evidence/oriental-voice-intake-release.md:9-12, 27-30).
*   **Partial-batch Semantics:** Reducer transactions retain independently valid fields, return rejected entries, and duplicate keys safely abort batches (.apr/evidence/oriental-voice-intake-release.md:24-26).
*   **API Submission & Review Persistence:** The verification marker is validated and safely stripped from `/api/leads`. Convex review heartbeats apply `leadId` changes only when explicitly provided, maintaining durable submissions (.apr/evidence/oriental-voice-intake-release.md:16-17, 20-21).
*   **Caption Retention & Primary-Action Visibility:** The primary action asserts in the initial viewport across a 15-size regression suite. In code, the live visual caption is placed visually above the action, correctly uses `aria-hidden` to rely on the transcript log, and retains the final string to prevent flickering (.apr/evidence/oriental-voice-intake-release.md:35-51; components/voice-agent/VoiceSessionStage.tsx:142-152, 168-185).
*   **Microphone Lifecycle:** The lifecycle hook polls permissions properly without assuming one-time/persistent state natively, and explicitly advises the visitor on browser-based every-visit configurations (.apr/evidence/oriental-voice-intake-release.md:52-54; components/voice-agent/VoiceSessionStage.tsx:186-189, 247-272).
*   **Telemetry Cap & Concurrent Routing:** PII-free per-tool samples enforce a 120-item boundary. Relying on array identity successfully ensures completed metrics (like `wait_for_user`) update review metadata even when length plateaus. Lead persistence routes concurrently with notification fan-outs (.apr/evidence/oriental-voice-intake-release.md:55-61, 104-107).
*   **Public-Health Verification & Exact Materialization:** The staging smoke independently expects `baseline/candidate(gpt-realtime-2.1)/low/adaptive` alongside `VOICE_VARIANT_PICKER=false`. Public health evaluates against the independent oracle before minted session execution (.apr/evidence/oriental-voice-intake-release.md:62-66, 116-120, 165-171).
*   **Production Isolation & Evaluation:** Evaluation separates failure boundaries and strips synthetic tests natively. The model gate remains `insufficient_data`, and production is strictly isolated and frozen on `.env` `baseline/control(gpt-realtime-2)` (.apr/evidence/oriental-voice-intake-release.md:22-23, 88-96, 118).

All telemetry is explicitly PII-free. There are no unproven claims, logical circularities, or production mutations.

VERDICT: SHIP SAFE DEFAULTS
