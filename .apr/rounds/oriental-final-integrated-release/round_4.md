This adversarial pre-merge review of the Oriental governed release confirms that all required application contracts, strict operational tooling guardrails, security requirements, and deployment protocols are implemented securely according to the spec. 

### Review Notes
1. **Explicit Secret Retirement & Fail-Before-Mutation:** The Infisical configuration syncing strictly matches the expected scope, successfully treating explicit `""` tombstones in `RETIRED_MANAGED_APPLICATION_ENVIRONMENT_KEYS` (`OWNER_AI`, `OWNER_CULTURAL`) as deletion signals. The API/host deployers securely query the previous `running:healthy` SHA boundary before updating control-plane environment variables or trigger paths (`scripts/deploy-coolify-production.ts:333`).
2. **Coolify Parity & Immutable Scope:** Coolify `PATCH` acknowledges exact multiline, runtime, and buildtime configuration scopes per token. Parity is aggressively enforced against `allowHiddenValues=true` read-backs without exposing sensitive values to the `scripts/deploy-coolify-production.ts` logs, preserving the least-privilege control.
3. **Fail-open Browser Storage:** All hardened browser configurations that deny `window.sessionStorage` or `localStorage` are safely handled via try/catch fail-open wrappers (`MerekaSiteLoader.tsx:22`, `handoff-memory.ts:39`, `GoogleAnalytics.tsx:292`).
4. **Principal-bound Authorization:** Interactive cookies, automated tasks, and privacy sweeps explicitly bind to specific principles (`interactive`, `automation`, `privacy`) and isolate roles dynamically without overlap (`lib/admin-permissions.ts:21`). The API correctly enforces a `isSameOriginJsonRequest` rule to reject basic forms/CSRF from triggering modifications (`admin-auth.ts:101`).
5. **Exact Native POST Login Fallback:** Implemented as a fail-closed 403 `csrf` layer when JavaScript fails. The Next.js API gracefully denies `application/x-www-form-urlencoded` submissions, protecting the bearer token from logging or URL reflections during a JS load failure (`admin-login-route.test.ts:74`).
6. **Data Privacy, Retention, & PII boundaries:**
   - **Sentry Data:** Scrubbing eliminates URL parameters (`queryFreeUrl`), exception details, and breadcrumb values cleanly before telemetry upload (`lib/sentry-privacy.ts`).
   - **Evaluations:** `piiFreeJudgeSummary` guarantees no name, email, org, or transcript extracts cross over into persistent records or Slack ops alerts. Bounded evaluations strictly consume local Redis rate-limiting guarantees to preclude runaway spend (`api/voice/debug/route.ts:74`).
   - **Deletion & Retention:** Implemented exactly to published windows (30/90/730 days) utilizing deterministic array bounds (`.take(24)` loops). Backfill/normalization requires `hasMore=false` drains. Privacy sweeps mandate verification of out-of-band ClickUp/Slack mirrors and correctly yield a `409` retryable status if incomplete.
7. **Public Analytics Consent Boundary:** Playwright verification enforces strict zero-data loading pre-consent and 100% telemetry muting across the `/admin` API dashboard (`scripts/release-verify.ts:160`).
8. **Intake Metrics:** `trackIntakeEvent` strips values securely, converting tracking to absolute numeric constraints and bounding attribution variants to strict type unions (`lib/client-analytics.ts`).  
9. **SLA Full-context Patch Verification:** Rechecking `app/api/admin/sla-check/route.ts` against the diff hunk labels affirms that `import { z } from "zod";` and the rest of the unmodified headers were context chunks. Nothing was accidentally dropped.

VERDICT: MERGE ORIENTAL RELEASE
