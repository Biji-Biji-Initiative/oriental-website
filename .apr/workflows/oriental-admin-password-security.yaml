name: oriental-admin-password-security
description: Exact-tree security review for Oriental interactive admin password management

documents:
  readme: .apr/evidence/oriental-admin-password-security.md
  spec: .apr/specs/oriental-admin-password-security.md
  implementation: .apr/evidence/oriental-admin-password-security.patch

oracle:
  model: "gpt-5.6-sol"

rounds:
  output_dir: .apr/rounds/oriental-admin-password-security
  impl_every_n: 1

template: |
  Perform an adversarial exact-tree security and release review of implementation
  commit 0a2321f8e003fade6f6a0c28d47d73a3f7b213df, tree
  861f29a18463f021e2f5c0a38255738e167c7d08, using the source-only patch and
  evidence manifest. The owner requires the reaffirmed password even though
  historical repository exposure means it is potentially known. Decide whether
  the signed method=password, viewer-only, thirty-minute session materially
  contains that risk now that it authorizes only a dedicated fixed aggregate
  metrics DTO and logout; all customer records, transcripts, voice detail,
  events, analytics/queues, and mutations require managed-token step-up.
  Recheck every prior blocker, especially round fourteen's reproduced semantic
  false greens: class property arrows; destructured object methods;
  Function.prototype bind/call/apply mediation; privileged functions stored on
  any-typed object properties; cross-file imported-helper return summaries; and
  safe local shadows of protected built-ins that were falsely rejected. Confirm
  a cached program-scoped, TypeScript symbol/declaration-keyed assignment and
  return-summary graph is shared by privileged loader identity, global
  Object/Reflect mutation identity, private authority taint, mutable-export
  identity, actual receiver resolution, and dynamic vector expansion, resolves
  declarations across the checker program, and keeps CI runtime bounded. Confirm
  assignment-backed aliases and shadowed names follow resolved symbols. Every
  exact and broader hostile fixture must fail across declarations, function
  expressions, arrows, class property arrows, object/class methods, static
  getters, instances, destructuring, any-object properties, imported helpers,
  and bind/call/apply mediation while safe reflected lookalikes, safe local
  shadows, safe receivers, safe vectors, harmless global mutations, and
  source-only export construction remain usable.
  Recheck aggregate bounds, whole-program authority admission, effective
  Next-config route extension governance, complete route/export closure,
  one-time private claims, collision cross-products, canonical-origin rejection,
  hard-fail release E2E, zero-skipped reporting, and safe telemetry.
  Inspect and recompute hashes for every attached raw artifact:
  github-evidence.json, integration-github-evidence.json, source-ci.log,
  integration-ci.log, integration-merge-dag.txt,
  source-to-integration-overlap.diff, integration-vitest.json,
  integration-audit.json, round_13.md, round_14.md, the final live PR/check
  evidence, and source-to-live-head.diff. Confirm source CI run 30413916916,
  job/check 90456091869, suite 82438933127, event pull_request, checked-out SHA
  0a2321f8e003fade6f6a0c28d47d73a3f7b213df, and tree
  861f29a18463f021e2f5c0a38255738e167c7d08. Confirm integration CI run
  30414193707, job/check 90456956044, suite 82439679922, event pull_request,
  checked-out commit 5839ef6ccd1214b1d5a90e6627e8e097236a4dd4, tree
  e637916cea01aa1e0540aed47ebd6144fcb672c6, all eight listed exact heads and
  first-parent composition, frozen pnpm 10.34.5 install, warning-free lint on
  the raw CI count of 294 files, strict TypeScript, zero production audit
  findings across 378 dependencies, all 89 files and 2,337 tests, Next.js
  16.2.12 build, and mobile performance.
  Compare the live PR head and source-to-live-head diff with the implementation
  commit and reject any non-APR descendant change. Confirm final live head
  92060bad1edac337393e5cc5bae5ede4c095c043, tree
  784207833079bdb1411fef9c6e8b887de9d23c98, CI run 30414882217,
  job/check 90459099573, and suite 82441530945 are exact-SHA successful, and
  independently verify that its descendant diff changes only `.apr/` evidence.
  Treat live cookie flags,
  password/viewer thirty-minute claims, password bearer rejection,
  review-token configured role and twelve-hour claims, Redis store/shared
  remaining-count exhaustion, raw-route 403s, managed HMAC readback,
  exact-SHA staging, Convex-first release, and guarded same-SHA production proof
  as mandatory post-merge gates; do not misstate them as already complete.
  End with `VERDICT: MERGE` or `VERDICT: DO NOT MERGE` on its own final line.

template_with_impl: |
  Perform an adversarial exact-tree security and release review of implementation
  commit 0a2321f8e003fade6f6a0c28d47d73a3f7b213df, tree
  861f29a18463f021e2f5c0a38255738e167c7d08, using the source-only patch and
  evidence manifest. The owner requires the reaffirmed password even though
  historical repository exposure means it is potentially known. Decide whether
  the signed method=password, viewer-only, thirty-minute session materially
  contains that risk now that it authorizes only a dedicated fixed aggregate
  metrics DTO and logout; all customer records, transcripts, voice detail,
  events, analytics/queues, and mutations require managed-token step-up.
  Recheck every prior blocker, especially round fourteen's reproduced semantic
  false greens: class property arrows; destructured object methods;
  Function.prototype bind/call/apply mediation; privileged functions stored on
  any-typed object properties; cross-file imported-helper return summaries; and
  safe local shadows of protected built-ins that were falsely rejected. Confirm
  a cached program-scoped, TypeScript symbol/declaration-keyed assignment and
  return-summary graph is shared by privileged loader identity, global
  Object/Reflect mutation identity, private authority taint, mutable-export
  identity, actual receiver resolution, and dynamic vector expansion, resolves
  declarations across the checker program, and keeps CI runtime bounded. Confirm
  assignment-backed aliases and shadowed names follow resolved symbols. Every
  exact and broader hostile fixture must fail across declarations, function
  expressions, arrows, class property arrows, object/class methods, static
  getters, instances, destructuring, any-object properties, imported helpers,
  and bind/call/apply mediation while safe reflected lookalikes, safe local
  shadows, safe receivers, safe vectors, harmless global mutations, and
  source-only export construction remain usable.
  Recheck aggregate bounds, whole-program authority admission, effective
  Next-config route extension governance, complete route/export closure,
  one-time private claims, collision cross-products, canonical-origin rejection,
  hard-fail release E2E, zero-skipped reporting, and safe telemetry.
  Inspect and recompute hashes for every attached raw artifact:
  github-evidence.json, integration-github-evidence.json, source-ci.log,
  integration-ci.log, integration-merge-dag.txt,
  source-to-integration-overlap.diff, integration-vitest.json,
  integration-audit.json, round_13.md, round_14.md, the final live PR/check
  evidence, and source-to-live-head.diff. Confirm source CI run 30413916916,
  job/check 90456091869, suite 82438933127, event pull_request, checked-out SHA
  0a2321f8e003fade6f6a0c28d47d73a3f7b213df, and tree
  861f29a18463f021e2f5c0a38255738e167c7d08. Confirm integration CI run
  30414193707, job/check 90456956044, suite 82439679922, event pull_request,
  checked-out commit 5839ef6ccd1214b1d5a90e6627e8e097236a4dd4, tree
  e637916cea01aa1e0540aed47ebd6144fcb672c6, all eight listed exact heads and
  first-parent composition, frozen pnpm 10.34.5 install, warning-free lint on
  the raw CI count of 294 files, strict TypeScript, zero production audit
  findings across 378 dependencies, all 89 files and 2,337 tests, Next.js
  16.2.12 build, and mobile performance.
  Compare the live PR head and source-to-live-head diff with the implementation
  commit and reject any non-APR descendant change. Confirm final live head
  92060bad1edac337393e5cc5bae5ede4c095c043, tree
  784207833079bdb1411fef9c6e8b887de9d23c98, CI run 30414882217,
  job/check 90459099573, and suite 82441530945 are exact-SHA successful, and
  independently verify that its descendant diff changes only `.apr/` evidence.
  Treat live cookie flags,
  password/viewer thirty-minute claims, password bearer rejection,
  review-token configured role and twelve-hour claims, Redis store/shared
  remaining-count exhaustion, raw-route 403s, managed HMAC readback,
  exact-SHA staging, Convex-first release, and guarded same-SHA production proof
  as mandatory post-merge gates; do not misstate them as already complete.
  End with `VERDICT: MERGE` or `VERDICT: DO NOT MERGE` on its own final line.
