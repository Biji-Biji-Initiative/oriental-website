The provided patch addresses the critical short-landscape email action visibility defect described in `.apr/specs/oriental-landscape-email-visibility.md:8-12`. The underlying issue is that the browser’s native focus-scroll behavior only brings the focused text input into the viewport. On viewports with constrained vertical real estate, such as 844x390, this native behavior often leaves the adjacent 44x44 Send control clipped or entirely out of bounds, resulting in a failing `0.9943181872367859` visibility ratio assertion during browser-backed tests.

To resolve this defect, the patch introduces a centralized `revealEmailEditor` function (`components/voice-agent/VoiceSessionStage.tsx:123-133`) and maps it symmetrically to both the input focus and blur events via `handleEmailFocus` and `handleEmailBlur` (`components/voice-agent/VoiceSessionStage.tsx:135-144`). When triggered, the logic employs a double `window.requestAnimationFrame` wrap. This is a deliberate and effective pattern: the first animation frame allows the browser’s native scroll-on-focus and React's touched/validation state mutations to fully commit to the DOM. The second frame then calculates the new layout bounds and safely fires the custom `scrollIntoView` call targeting the outer container (`components/voice-agent/VoiceSessionStage.tsx:128-131`). This approach completely bypasses arbitrary `setTimeout` delays and correctly anchors the viewport to the entire editor wrapper via `closest("[data-email-quick-capture]")`.

By using `block: "nearest"`, the script enforces the minimal amount of vertical scrolling required to pull the complete wrapper into the viewport. The automated intersection observer assertion described in the specification strictly expects the visibility ratio to be exactly `1`. A failure ratio of `0.9943181872367859` means a fraction of the component—typically the lowest edge of the adjacent Send button—is left clipped off-screen by the browser’s default behavior. To guarantee that the ratio-1 assertion reliably passes, the patch smartly injects the `scroll-my-2` utility class directly onto the container element (`components/voice-agent/VoiceSessionStage.tsx:235`). This applies an 8px (0.5rem) scroll margin to the top and bottom of the element, preventing the outer boundary of the container from resting flush against the mathematical edge of the viewport. Consequently, the minimum 44x44 CSS-pixel target remains fully visible and intact for user interaction without requiring any intrusive changes to the fixed DOM dimensions, internal padding, or overarching visual layout.

Additionally, this patch successfully preserves all necessary breakpoint and validation behaviors. The container retains its `lg:hidden` class (`components/voice-agent/VoiceSessionStage.tsx:235`), ensuring that on viewports of 1024px and wider (e.g., the tested 1024x390 and 1024x600 breakpoints), the mobile-specific container correctly hides itself and focus transfers are completely unimpacted. The critical validation hooks `onEmailFocus?.()` and `onEmailBlur?.()` are explicitly preserved and invoked before the `revealEmailEditor` function in both event handlers (`components/voice-agent/VoiceSessionStage.tsx:136`, `components/voice-agent/VoiceSessionStage.tsx:141`). This sequence correctly delegates back to the parent to manage upstream state before calculating scroll coordinates, ensuring that telemetry capture, touch state updates, and form validations function identically to the previous version without introducing regressions.

In regards to potential regressions like focus theft, scroll loops, or horizontal overflow, the changes are highly insulated:
1. **Focus Theft**: The `scrollIntoView` API merely adjusts the scroll offset of parent containers; it strictly does not modify the `document.activeElement`. Calling it inside `handleEmailFocus` (`components/voice-agent/VoiceSessionStage.tsx:261`) ensures focus correctly remains locked on the email input while the viewport seamlessly shifts around it.
2. **Scroll Loops**: Scroll loops typically occur when scroll events trigger side effects that cause further scrolling. Because `revealEmailEditor` is securely bound exclusively to explicit `onFocus` and `onBlur` UI events, and the scroll instruction utilizes `{ behavior: "instant" }` (`components/voice-agent/VoiceSessionStage.tsx:130`), the component cleanly avoids initiating smooth scroll animations that might fire intermediate intersection observer events, thereby cutting off any avenue for cyclic scroll behavior.
3. **Horizontal Overflow**: The patch preserves `inline: "nearest"` in the scroll function (`components/voice-agent/VoiceSessionStage.tsx:130`) which strictly prevents arbitrary horizontal jumps. Furthermore, the `w-full max-w-xl` dimensions remain untouched (`components/voice-agent/VoiceSessionStage.tsx:235`). Because the elements cannot horizontally expand beyond the viewport bounds, the `scrollIntoView` execution is structurally incapable of causing horizontal overflow.

In summation, this is a tightly scoped and robust patch. It expertly solves the short-landscape layout clipping issue by correcting the viewport around the whole action block instead of isolating just the input. The solution maintains perfect parity with all previous test requirements, UI bounds, validations, and accessibility standards without risking adversarial side effects.

VERDICT: MERGE LANDSCAPE EMAIL FIX
