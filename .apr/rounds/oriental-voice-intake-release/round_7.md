Here is the evaluation of the implementation evidence against the voice intake release contract:

*   **Failure classification & retry selection**: `useRealtimeVoiceSession.ts` properly parses cloned SDP responses, classifies them before calling the retry policy, accepts only `realtime_busy`, and explicitly rejects `realtime_quota_exhausted` for retry (.apr/evidence/oriental-voice-intake-release.md:5-8, 158-161).
*   **Typed interruption**: Typed turns serialize cancel, clear, text, and response. Expected no-active-response cancellation races are filtered from actionable warnings (.apr/evidence/oriental-voice-intake-release.md:18-19).
*   **Adaptive/strict email grounding & correction**: Adaptive mode strictly requires syntax-valid, model-evidence-grounded bounds and an explicit email cue or exact latest-turn match. Strict mode retains exact readback/confirmation requirements. Corrections safely invalidate and re-evaluate prior evidence (.apr/evidence/oriental-voice-intake-release.md:9-12, 29-30).
*   **API submission**: The verification marker for voice source is strictly required and successfully stripped before persistence (.apr/evidence/oriental-voice-intake-release.md:16-17).
*   **Review persistence**: Convex heartbeats update `leadId` only when provided to preserve durable submission linkage. Snapshots securely persist only PII-free capture mode and verification provenance (.apr/evidence/oriental-voice-intake-release.md:20-21, 31-34). 
*   **Partial-batch semantics**: Multi-field capture processes via a single reducer transaction. It retains independently valid fields, returns rejected fields for targeted retry, and safely aborts duplicate keys before commit (.apr/evidence/oriental-voice-intake-release.md:24-26).
*   **Bounded ASR identity grounding**: Native-audio name drift enforces an explicit field cue combined with a one-edit phonetic skeleton, proven by a negative regression on distinct names with the same initial (.apr/evidence/oriental-voice-intake-release.md:27-29).
*   **Initial primary-action visibility**: Responsive regressions reset layout across a full matrix up to 1440x900, asserting the primary action remains strictly in the initial scroll region before scrolling (.apr/evidence/oriental-voice-intake-release.md:35-43, 50-51, 121-124).
*   **Streaming/final caption retention**: The live `aria-hidden` visual caption correctly renders visually above the primary action. Between turns, the `lastAssistantLine` keeps completed questions visible instead of vanishing (components/voice-agent/VoiceSessionStage.tsx:139-182).
*   **Microphone lifecycle**: The copy explicitly educates on every-visit vs. one-time access without assuming history, referencing the `prompt` permission state natively, and tracks are safely released on close (.apr/evidence/oriental-voice-intake-release.md:52-54; components/voice-agent/VoiceSessionStage.tsx:183-187, 249-269).
*   **Per-tool telemetry (120-sample cap)**: Latency samples are strictly PII-free. The full 120-sample cap reliably triggers metadata publication during array replacement due to proper array identity mapping rather than unsafe array-length comparisons (.apr/evidence/oriental-voice-intake-release.md:55-59, 104-107).
*   **Concurrent routing**: Lead persistence and notification fan-out start concurrently, preserving prior durability semantics without blocking (.apr/evidence/oriental-voice-intake-release.md:59-61).
*   **Exact five-field staging-cell materialization & Infisical parity**: Automated Infisical environments assert explicit same-directory rendered environments. Staging reliably reports `baseline/candidate(gpt-realtime-2.1)/low/adaptive` with the picker securely disabled (`false`). Secrets are confirmed unleaked (.apr/evidence/oriental-voice-intake-release.md:108-120).
*   **Public-health verification**: The tautology failure from APR round 5 is fully resolved. Staging explicitly references the `gpt-realtime-2.1` candidate cell as its oracle, verifies health first against that contract, and then checks the minted session independently (.apr/evidence/oriental-voice-intake-release.md:62-66, 165-171).
*   **Production isolation**: Validated. Production safely remains frozen on `bb8e2673...` (`baseline/control(gpt-realtime-2)/low/adaptive`). Production host paths actively reject the candidate environment. The model-only reasoning preview maintains `insufficient_data` and stops short of public release (.apr/evidence/oriental-voice-intake-release.md:88-96, 118-120).
*   **Evaluation attribution**: Metrics strictly separate failure reasons and synthetic reserved smokes (e.g. `qa.nebula@example.test`) are excluded from customer-quality aggregates without filtering actual testing traffic (.apr/evidence/oriental-voice-intake-release.md:22-23).
*   **Mereka Mini Mark Refactor**: The ambiguous `MiniOrb` component is thoroughly removed. `MerekaMiniMark` renders appropriately in idle/submitted DOM stages with verified path-and-dot geometry (.apr/evidence/oriental-voice-intake-release.md:175-183; components/voice-agent/VoiceSessionStage.tsx:6, 24, 105).

All critical blockers—unproven claims, PII leakage, reasoning promotion, candidate leaks, and production mutation—are explicitly resolved and verified by executable tests, clean checks, and component behavior. 

VERDICT: SHIP SAFE DEFAULTS
