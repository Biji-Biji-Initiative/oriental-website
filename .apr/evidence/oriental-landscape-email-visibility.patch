diff --git a/components/voice-agent/VoiceSessionStage.tsx b/components/voice-agent/VoiceSessionStage.tsx
index 0454113..d65b4f0 100644
--- a/components/voice-agent/VoiceSessionStage.tsx
+++ b/components/voice-agent/VoiceSessionStage.tsx
@@ -120,13 +120,11 @@ export function VoiceSessionStage({
     if (onSendText(text)) setDraft("");
   };
 
-  const handleEmailBlur = () => {
-    onEmailBlur?.();
+  const revealEmailEditor = () => {
     window.requestAnimationFrame(() => {
-      // Let the touched/verification state commit first, then reveal the whole
-      // compact editor. scrollIntoView accounts for every clipping ancestor;
-      // checking only the outer dialog can still leave the helper hidden in a
-      // short landscape viewport.
+      // Let focus/touched state commit first, then reveal the whole compact
+      // editor. A focused input can be visible while the adjacent Send button
+      // is still clipped at short landscape heights.
       window.requestAnimationFrame(() => {
         const quickCapture = emailInputRef?.current?.closest<HTMLElement>("[data-email-quick-capture]");
         quickCapture?.scrollIntoView({ behavior: "instant", block: "nearest", inline: "nearest" });
@@ -134,6 +132,16 @@ export function VoiceSessionStage({
     });
   };
 
+  const handleEmailFocus = () => {
+    onEmailFocus?.();
+    revealEmailEditor();
+  };
+
+  const handleEmailBlur = () => {
+    onEmailBlur?.();
+    revealEmailEditor();
+  };
+
   if (status === "submitted") {
     return (
       <div className="grid h-full min-h-[520px] place-items-center text-center">
@@ -224,7 +232,7 @@ export function VoiceSessionStage({
 
         <div
           className={cn(
-            "mt-5 w-full max-w-xl rounded-xl border bg-white/[0.045] p-3 text-left lg:hidden",
+            "mt-5 w-full max-w-xl scroll-my-2 rounded-xl border bg-white/[0.045] p-3 text-left lg:hidden",
             showEmailError
               ? "border-mk-error/65 shadow-[0_0_0_1px_rgba(255,122,122,0.12)]"
               : emailAttention
@@ -250,7 +258,7 @@ export function VoiceSessionStage({
               id="voice-quick-email"
               onBlur={handleEmailBlur}
               onChange={(event) => onEmailChange?.(event.target.value)}
-              onFocus={onEmailFocus}
+              onFocus={handleEmailFocus}
               placeholder="name@example.com"
               ref={emailInputRef}
               required
